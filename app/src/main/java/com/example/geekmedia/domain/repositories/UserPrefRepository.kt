package com.example.geekmedia.domain.repositories

import android.net.Uri
import androidx.datastore.preferences.core.Preferences
import kotlinx.coroutines.flow.Flow
import com.example.geekmedia.domain.Status

interface UserPrefRepository {

//    suspend fun saveRegisteredUser(isRegistered: Boolean)

//    suspend fun saveUserAvatar(imageUri: Uri) : Flow<Status<Uri>>
//
//    suspend fun getUserAvatar() : Flow<Status<String>>

}